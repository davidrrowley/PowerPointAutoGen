# OpenAI provider artefacts

This folder is reserved for:
- agent runtime configs
- handoff maps
- any OpenAI-specific agent definitions

`AGENTS.md` at repo root provides the default context entry point.
