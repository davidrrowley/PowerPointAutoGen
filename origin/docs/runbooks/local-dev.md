# Local development

## Prerequisites
- Node.js LTS
- Git
- PowerShell (Windows) or Bash (macOS/Linux)

## Start here
- `apps/web` for the UI
- `apps/api` for the API
- `infra/local` for local dependencies

## Environment
Copy `.env.example` to `.env` and adjust as needed.
