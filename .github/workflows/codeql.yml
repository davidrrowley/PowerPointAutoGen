name: CodeQL

on:
  workflow_dispatch:
  schedule:
    - cron: "0 3 * * 1"

jobs:
  analyze:
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@v4
      - name: CodeQL guidance
        run: |
          echo "Enable CodeQL by adding languages and build steps."
